'use client';

import React from 'react';
import { Shimmer } from 'react-shimmer';

const CardItemShimmer: React.FC = (props) => {
  return <Shimmer height={310} width={420} />;
};

export { CardItemShimmer };
